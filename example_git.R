#git clone url
# To switch branch we can checkout
# git checkout branch_name

# If the branch name does not exist type -b
# git checkout -b branch_name]

# For adding files
# git add file_name

# Write a commit Message
# git commit -m "Message"